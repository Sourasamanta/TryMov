# TryMov
TryMov is a movie recommendation app combining an Android client (Kotlin + Jetpack Compose) and a FastAPI backend. It uses the TMDB 5000 dataset with TF‑IDF and cosine similarity to suggest similar movies and fetches posters via OMDb.
